<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.myWeb.www.repository.CommentDAO">
  	<insert id="post">
  		insert into comment(bno, writer, content) values (#{bno}, #{writer}, #{content})
  	</insert>
  	
  	<select id="spread" resultType="com.myWeb.www.domain.CommentVO">
  		select * from comment where bno = #{bno} order by cno desc limit #{pgvo.pageStart}, #{pgvo.qty}
  	</select>
  	
  	<select id="selectOneBnoTotalCount" resultType="int">
  		select count(*) from comment where bno = #{bno}
  	</select>
  	 
  	<update id="modify">
  		update comment set content = #{content}, mod_date = now() where cno = #{cno}
  	</update>
  	
  	<delete id="delete">
  		delete from comment where cno = #{cno}
  	</delete>
  	
  </mapper>